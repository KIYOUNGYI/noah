spring:
  profiles:
    active: local
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://_dev-db.glowpick.net:3306/user_glowmee?autoReconnect=true&characterEncoding=utf8&useUnicode=true&useSSL=false
    username: glowpick
    password: djWotjtlfgodehla?
#    hikari:
#      connection-timeout: 20000
#        minimum-idle: 5
#        maximum-pool-size: 12
#        idle-timeout: 3000000
#        max-lifetime: 1200000

  jpa:
    hibernate:
      ddl-auto: none # 건들지 마세용
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    generate-ddl: false # 건들지 마세용
    show-sql: false
    database: mysql
    database-platform: app.noah.config.MySQLDialectCustom
    properties:
      hibernate:
        format_sql: false
        enable_lazy_load_no_trans: true
#        search:
#          default:
#            directory_provider: filesystem
#            indexBase: lucene/indexes/
#          lucene_version: LUCENE_CURRENT
#          indexing_strategy: event

